<div class="page-header">
  <!--  link_to 'New Task', new_task_path, :class => "btn btn-info pull-right" -->
  <h1>Tasks for <%= @project.name %></h1>
</div>

<ol class="breadcrumb">
  <li><a href="/projects">Projects</a></li>
  <li><%= link_to @project.name, project_path(@project) %></li>
  <li class="active"><%= @project.name %>'s Tasks</li>
</ol>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Description</th>
      <th>Due On</th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
    <tr>
      <td><%= link_to task.description, task %></td>
      <td><%= task.date %></td>
      <td><div class="text-right"><%= link_to 'Edit', edit_task_path(task), :class => "btn btn-info" %>
        <%= link_to 'Delete', task, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></div></td>
      </tr>
      <% end %>
    </tbody>
  </table>
